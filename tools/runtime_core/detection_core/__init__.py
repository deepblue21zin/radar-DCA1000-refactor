"""Detection pipeline building blocks.

The public compatibility surface remains ``tools.runtime_core.detection``.
This package holds smaller implementation units so detector math, tracing,
and body-center refinement can evolve independently.
"""

